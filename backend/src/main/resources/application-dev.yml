spring:
  flyway:
    locations: classpath:db/migration,classpath:db/dev-seed
  config:
    activate:
      on-profile: dev

invsys:
  dev:
    auto-seed: true
  security:
    cookie-secure: false
    cookie-same-site: Lax
  redis:
    enabled: false
